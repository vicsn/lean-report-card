project_id         = "your-gcp-project"
app_repo_url       = "https://github.com/vicsn/lean-report-card.git"
app_git_ref        = "main"
notification_email = "ops@example.com"

# Required to clone this private repository onto the VM. Also raises GitHub API
# rate limits. It is written to Secret Manager.
# github_token = "github_pat_..."

# Optional override for the compiled-in public PostHog project token.
# posthog_project_token = "phc_..."
# posthog_host          = "https://us.i.posthog.com"
